# Encyclopedia
A space to organize the concepts I have learned.

1. API(Application Programming Interface)
  You interact with something... like.. for example Television's button.

REST API use URL for button like this:
GET, POST, DELETE, PUT(these are http MEthods) /2/lists/:id

3. REST api's problem
  1. over-fetching
     :recieiving a lot of data whethet use it or not.
       GraphQL lets you send GraphQL queries to an API and get exactly what you ASK FOR.
  2. under-fetching
     :recieiving less data than you need.
